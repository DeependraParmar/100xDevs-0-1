// Functions are independent abstract code for performing a specific tasks. Functions are basically a reusable codebox, which takes an input, returns an output. A function can also takes a function as inputs, callbacks.

function sum(a,b){
    return a+b;
}

const sumValue = sum(19,29);
console.log(sumValue);